module.exports = {
    mode: 'jit',
    purge: ['./index.html', './src/**/*.{js,ts,vue,html}'], // Ajusta las rutas según tus archivos
    darkMode: false, // or 'media' or 'class'
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
}
